# GPU Feature Extraction Requirements (Windows / RTX 3050)
# Install via: pip install -r requirements_gpu.txt

# PyTorch mapped to CUDA 12.1 (compatible with Driver 555.99 / CUDA 12.5)
--extra-index-url https://download.pytorch.org/whl/cu121
torch==2.3.0+cu121
torchvision==0.18.0+cu121

# Facial Landmark tracking (PyTorch based)
face-alignment>=1.3.5
opencv-python>=4.8.0

# General processing
numpy>=1.24.0
pandas>=2.0.0
tqdm>=4.65.0
h5py>=3.8.0
scipy>=1.10.0
